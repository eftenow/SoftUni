function strLength(first, second, third){
    totalCharacters = first.length + second.length + third.length;
    avgCharacters = Math.floor(totalCharacters / 3);
    console.log(totalCharacters);
    console.log(avgCharacters);
    
}

strLength('chocolate', 'ice cream', 'cake')